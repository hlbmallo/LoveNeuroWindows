<Page
    x:Class="NerveCentreW10.Views.Section8MoreResources"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:NerveCentreW10.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="0.5*"/>
            <RowDefinition Height="0.5*"/>
        </Grid.RowDefinitions>

        <!--<GridView x:Name="GridView1" Style="{StaticResource GridViewStyle1}" ItemTemplate="{StaticResource VideoTutorialsDataTemplate}" ItemClick="GridView1_ItemClick" ItemsSource="{x:Bind ViewModel.MoreResourcesList}" ItemContainerStyle="{StaticResource GridViewItemStyle1}"/>-->

        <GridView x:Name="GridView2" Style="{StaticResource GridViewStyle1}" ItemTemplate="{StaticResource MoreResourcesDataTemplate}" ItemClick="GridView2_ItemClick" ItemsSource="{x:Bind ViewModel.MoreResourcesList}" ItemContainerStyle="{StaticResource GridViewItemStyle1}"/>

    </Grid>
    
</Page>
