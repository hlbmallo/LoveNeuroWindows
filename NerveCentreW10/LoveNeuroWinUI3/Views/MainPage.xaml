<Page
    x:Class="LoveNeuroWinUI3.Views.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="using:Microsoft.Toolkit.Uwp.UI.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid x:Name="ContentArea" Margin="5">
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Grid Grid.Row="0">
            <controls:ImageEx CornerRadius="10" Source="ms-appx:///Assets/Others/Welcome7.png" />
        </Grid>
        <StackPanel
            Grid.Row="1"
            Margin="10"
            HorizontalAlignment="Center"
            Orientation="Horizontal">
            <Button
                Margin="5"
                HorizontalAlignment="Center"
                VerticalAlignment="Bottom"
                Click="Button_Click"
                Content="Play Intro Video" />
            <Button
                x:Name="RateAndReviewButton"
                Margin="5"
                HorizontalAlignment="Center"
                VerticalAlignment="Bottom"
                Background="#ff7201"
                Click="RateAndReviewButton_Click"
                Content="Rate and Review" />
        </StackPanel>


    </Grid>
</Page>
